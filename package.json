{
  "name": "pitaster",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter @pitaster/electron dev",
    "build": "bun run --workspaces build",
    "typecheck:all": "bun run --workspaces typecheck",
    "test": "bun test",
    "sync:skills": "bun run scripts/generate-seed-content.ts"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "typescript": "^5.5.0"
  }
}
